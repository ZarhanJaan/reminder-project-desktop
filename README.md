# 📝 Reminder Project Desktop

A simple desktop application to manage reminders and tasks.  
Built with **Python** and **Tkinter** for the GUI, with reminders stored in a local JSON file.

---

## 📖 Description
Reminder Project Desktop helps you create, view, and delete reminders with a clean desktop interface.  
Reminders are saved persistently in `reminders.json` and can be managed directly from the app.

---

## ✨ Features
- Add new reminders with title, description, and date/time
- View all reminders in a scrollable text area
- Delete reminders by title
- Persistent storage using JSON
- User‑friendly interface built with Tkinter

---

## 🛠️ Tech Stack
- **Language:** Python 3
- **GUI Framework:** Tkinter
- **Storage:** JSON file (`reminderss.json`)

---

## ⚙️ Installation & Setup

### 1. Clone Repository
```bash
git clone https://github.com/ZarhanJaan/reminder-project-desktop.git
cd reminder-project-desktop
